export type SupplierType = {
    id: number,
    name: string,
    email: string,
}

export type TaxType = {
  id: number,
  name: string,
  rate: string,
}

export type CategoryType = {
  id: number,
  name: string,
}